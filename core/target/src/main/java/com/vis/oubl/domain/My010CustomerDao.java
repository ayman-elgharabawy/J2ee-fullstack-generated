// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand!
// Generated by: SpringDao.vsl in andromda-spring-cartridge.
//
package com.vis.oubl.domain;

import com.vis.oubl.Search;
import com.vis.oubl.vo.My010CustomerVO;
import java.util.Collection;
import java.util.Set;
import org.andromda.spring.PaginationResult;

/**
 *
 * @see My010Customer
 */
public interface My010CustomerDao
{
    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p>
     * This specific flag denotes no transformation will occur.
     */
    public static final int TRANSFORM_NONE = 0;


    /**
     * Transforms the given results to a collection of {@link My010Customer}
     * instances (this is useful when the returned results contains a row of data and you want just entities only).
     *
     * @param results the query results.
     */
    public void toEntities(final Collection<?> results);

    /**
     * This constant is used as a transformation flag; entities can be converted automatically into value objects
     * or other types, different methods in a class implementing this interface support this feature: look for
     * an <code>int</code> parameter called <code>transform</code>.
     * <p>
     * This specific flag denotes entities must be transformed into objects of type
     * {@link My010CustomerVO}.
     */
    public static final int TRANSFORM_MY010CUSTOMERVO = 1;

    /**
     * Copies the fields of the specified entity to the target value object. This method is similar to
     * toMy010CustomerVO(), but it does not handle any attributes in the target
     * value object that are "read-only" (as those do not have setter methods exposed).
     * @param source
     * @param target
     */
    public void toMy010CustomerVO(
        My010Customer source,
        My010CustomerVO target);

    /**
     * Converts this DAO's entity to an object of type {@link My010CustomerVO}.
     * @param entity
     * @return My010CustomerVO
     */
    public My010CustomerVO toMy010CustomerVO(My010Customer entity);

    /**
     * Converts this DAO's entity to a Collection of instances of type {@link My010CustomerVO}.
     * @param entities
     */
    public Collection<My010CustomerVO> toMy010CustomerVOCollection(Collection<?> entities);

    /**
     * Converts this DAO's entity to an array of instances of type {@link My010CustomerVO}.
     * @param entities
     * @return My010CustomerVO[]
     */
    public My010CustomerVO[] toMy010CustomerVOArray(Collection<?> entities);

    /**
     * Copies the fields of {@link My010CustomerVO} to the specified entity.
     * @param source
     * @param target
     * @param copyIfNull If FALSE, the value object's field will not be copied to the entity if the value is NULL. If TRUE,
     * it will be copied regardless of its value.
     */
    public void my010CustomerVOToEntity(
        My010CustomerVO source,
        My010Customer target,
        boolean copyIfNull);

    /**
     * Converts an instance of type {@link My010CustomerVO} to this DAO's entity.
     * @param my010CustomerVO
     * @return My010Customer
     */
    public My010Customer my010CustomerVOToEntity(My010CustomerVO my010CustomerVO);

    /**
     * Converts a Collection of instances of type {@link My010CustomerVO} to this
     * DAO's entity.
     * @param instances
     */
    public void my010CustomerVOToEntityCollection(Collection<?> instances);

    /**
     * Gets an instance of My010Customer from the persistent store.
     * @param id
     * @return My010Customer
     */
    public My010Customer get(Long id);

    /**
     * <p>
     * Does the same thing as {@link #get(Long)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     * optionally transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform flag to determine transformation type.
     * @param id the identifier of the entity to get.
     * @return either the entity or the object transformed from the entity.
     */
    public Object get(int transform, Long id);

    /**
     * Loads an instance of My010Customer from the persistent store.
     * @param id
     * @return My010Customer
     */
    public My010Customer load(Long id);

    /**
     * <p>
     * Does the same thing as {@link #load(Long)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined in this class then the result <strong>WILL BE</strong> passed through an operation which can
     * optionally transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform flag to determine transformation type.
     * @param id the identifier of the entity to load.
     * @return either the entity or the object transformed from the entity.
     */
    public Object load(int transform, Long id);

    /**
     * Loads all entities of type {@link My010Customer}.
     *
     * @return the loaded entities.
     */
    public Collection<My010Customer> loadAll();

    /**
     * <p>
     * Does the same thing as {@link #loadAll()} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     *
     * @param transform the flag indicating what transformation to use.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int transform);

    /**
     * <p>
     * Does the same thing as {@link #loadAll()} with an
     * additional two arguments called <code>pageNumber</code> and <code>pageSize</code>. The <code>pageNumber</code>
     * argument allows you to specify the page number when you are paging the results and the pageSize allows you to specify the size of the
     * page retrieved.
     * </p>
     *
     * @param pageNumber the page number to retrieve when paging results.
     * @param pageSize the size of the page to retrieve when paging results.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int pageNumber, final int pageSize);

    /**
     * <p>
     * Does the same thing as {@link #loadAll(int)} with an
     * additional two arguments called <code>pageNumber</code> and <code>pageSize</code>. The <code>pageNumber</code>
     * argument allows you to specify the page number when you are paging the results and the pageSize allows you to specify the size of the
     * page retrieved.
     * </p>
     *
     * @param transform the flag indicating what transformation to use.
     * @param pageNumber the page number to retrieve when paging results.
     * @param pageSize the size of the page to retrieve when paging results.
     * @return the loaded entities.
     */
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize);

    /**
     * Creates an instance of My010Customer and adds it to the persistent store.
     * @param my010Customer
     * @return My010Customer
     */
    public My010Customer create(My010Customer my010Customer);

    /**
     * <p>
     * Does the same thing as {@link #create(My010Customer)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param my010Customer
     * @return Object
     */
    public Object create(int transform, My010Customer my010Customer);

    /**
     * Creates a new instance of My010Customer and adds
     * from the passed in <code>entities</code> collection
     *
     * @param entities the collection of My010Customer
     * instances to create.
     *
     * @return the created instances.
     */
    public Collection<My010Customer> create(Collection<My010Customer> entities);

    /**
     * <p>
     * Does the same thing as {@link #create(My010Customer)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entities (into value objects for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param entities
     * @return Collection
     */
    public Collection<?> create(int transform, Collection<My010Customer> entities);

    /**
     * <p>
     * Creates a new <code>My010Customer</code>
     * instance from <strong>all</strong> attributes and adds it to
     * the persistent store.
     * </p>
     * @param firstName 
     * @param lastName 
     * @param middleName 
     * @param identityNumber 
     * @param insuranceNumber 
     * @param address 
     * @param mobileNumber 
     * @param telephoneNumber 
     * @param nationality 
     * @param type 
     * @return My010Customer
     */
    public My010Customer create(
        String firstName,
        String lastName,
        String middleName,
        Integer identityNumber,
        Integer insuranceNumber,
        String address,
        Long mobileNumber,
        Integer telephoneNumber,
        String nationality,
        String type);

    /**
     * <p>
     * Does the same thing as {@link #create(String, String, String, Integer, Integer, String, Long, Integer, String, String)} with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * the returned entity will <strong>NOT</strong> be transformed. If this flag is any of the other constants
     * defined here then the result <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entity (into a value object for example). By default, transformation does
     * not occur.
     * </p>
     * @param transform
     * @param firstName 
     * @param lastName 
     * @param middleName 
     * @param identityNumber 
     * @param insuranceNumber 
     * @param address 
     * @param mobileNumber 
     * @param telephoneNumber 
     * @param nationality 
     * @param type 
     * @return My010Customer
     */
    public Object create(
        int transform,
        String firstName,
        String lastName,
        String middleName,
        Integer identityNumber,
        Integer insuranceNumber,
        String address,
        Long mobileNumber,
        Integer telephoneNumber,
        String nationality,
        String type);


    /**
     * Updates the <code>my010Customer</code> instance in the persistent store.
     * @param my010Customer
     */
    public void update(My010Customer my010Customer);

    /**
     * Updates all instances in the <code>entities</code> collection in the persistent store.
     * @param entities
     */
    public void update(Collection<My010Customer> entities);

    /**
     * Removes the instance of My010Customer from the persistent store.
     * @param my010Customer
     */
    public void remove(My010Customer my010Customer);

    /**
     * Removes the instance of My010Customer having the given
     * <code>identifier</code> from the persistent store.
     * @param id
     */
    public void remove(Long id);

    /**
     * Removes all entities in the given <code>entities</code> collection.
     * @param entities
     */
    public void remove(Collection<My010Customer> entities);

    /**
     * 
     * @return My010CustomerVO[]
     */
    public My010CustomerVO[] queryAllCustomer();

    /**
     * 
     * @param telephoneNumber 
     * @return My010CustomerVO
     */
    public My010CustomerVO getCustomerByTelephoneNumber(Long telephoneNumber);

    /**
     * 
     * @param id 
     * @return My010CustomerVO
     */
    public My010CustomerVO getCustomertById(Long id);


    /**
     * Does the same thing as {@link #search(int, Search)} but with an
     * additional two flags called <code>pageNumber</code> and <code>pageSize</code>. These flags allow you to
     * limit your data to a specified page number and size.
     *
     * @param transform the transformation flag.
     * @param pageNumber the page number in the data to retrieve
     * @param pageSize the size of the page to retrieve.
     * @param search the search object which provides the search parameters and pagination specification.
     * @return any found results from the search wrapped in a {@link PaginationResult} instance.
     */
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search);

    /**
     * Does the same thing as {@link #search(Search)} but with an
     * additional two flags called <code>pageNumber</code> and <code>pageSize</code>. These flags allow you to
     * limit your data to a specified page number and size.
     *
     * @param pageNumber the page number in the data to retrieve
     * @param pageSize the size of the page to retrieve.
     * @param search the search object which provides the search parameters and pagination specification.
     * @return any found results from the search wrapped in a {@link PaginationResult} instance.
     */
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search);

    /**
     * Does the same thing as {@link #search(Search)} but with an
     * additional flag called <code>transform</code>. If this flag is set to <code>TRANSFORM_NONE</code> then
     * finder results will <strong>NOT</strong> be transformed during retrieval.
     * If this flag is any of the other constants defined here
     * then results <strong>WILL BE</strong> passed through an operation which can optionally
     * transform the entities (into value objects for example). By default, transformation does
     * not occur.
     *
     * @param transform the transformation flag.
     * @param search the search object which provides the search parameters and pagination specification.
     * @return any found results from the search.
     */
    public Set<?> search(final int transform, final Search search);

    /**
     * Performs a search using the parameters specified in the given <code>search</code> object.
     *
     * @param search the search object which provides the search parameters and pagination specification.
     * @return any found results from the search.
     */
    public Set<My010Customer> search(final Search search);

    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>com.vis.oubl.domain.My010CustomerDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     * <p>
     * This method will return instances of these types:
     * <ul>
     *   <li>{@link My010Customer} - {@link #TRANSFORM_NONE}</li>
     *   <li>{@link My010CustomerVO} - {@link #TRANSFORM_MY010CUSTOMERVO}</li>
     * </ul>
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link com.vis.oubl.domain.My010CustomerDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see #transformEntities(int,Collection)
     */
    public Object transformEntity(final int transform, final My010Customer entity);

    /**
     * Transforms a collection of entities using the
     * {@link #transformEntity(int,My010Customer)}
     * method. This method does not instantiate a new collection.
     * <p>
     * This method is to be used internally only.
     *
     * @param transform one of the constants declared in <code>com.vis.oubl.domain.My010CustomerDao</code>
     * @param entities the collection of entities to transform
     * @see #transformEntity(int,My010Customer)
     */
    public void transformEntities(final int transform, final Collection<?> entities);

    // spring-dao merge-point
}